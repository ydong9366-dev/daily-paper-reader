---
title: "SAPO: Single-Rollout Autoregressive Policy Optimization for Agentic Reinforcement Learning"
title_zh: SAPO：用于智能体强化学习的单次轨迹自回归策略优化
authors: "Dayang Liang, Lang Feng, Bo An, Yunlong Liu"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.19842v1"
tags: ["query:agents"]
score: 9.0
evidence: 面向智能体强化学习的单次轨迹自回归策略优化
tldr: "针对现有智能体强化学习方法在长程任务中缺乏价值泛化、时序信用分配不佳以及采样效率低的问题，提出SAPO框架，让策略与价值函数共享单一自回归主干，在因果边界处分别输出策略与价值预测，并结合轨迹级广义优势估计器。在ALFWorld和WebShop上超过PPO和GRPO约15个点，同时省去独立评论家模型，减少33.2%的每轮耗时。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.19842v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1404, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19842v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1430, \"height\": 604, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.19842v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 803, \"label\": \"Table\"}]"
motivation: 现有无评论家方法依赖多次采样估计优势，存在价值泛化缺失、优势坍塌及采样预算与性能冲突的局限。
method: 让策略和价值共享单一自回归网络，在因果边界分别输出预测，联合优化PPO和SARSA目标，并用归一化的lambda-return估计轨迹优势。
result: "在ALFWorld和WebShop上，SAPO平均比PPO和GRPO高15.1和12.1个百分点，且省去独立评论家模型、比PPO每轮快33.2%。"
conclusion: SAPO以轻量自回归方式同时获得稳定训练和高效推理，为智能体RL提供了低成本高回报的解决方案。
---

## 摘要
智能体强化学习已成为大型语言模型后训练中的关键阶段。现有的无评论家、基于组相对的方法通过多次轨迹估计策略优势，避免了传统近端策略优化（PPO）的显著内存开销，并在长程交互任务上取得了强劲性能。尽管取得了成功，近期研究揭示了三个局限性：（1）缺乏显式的价值泛化和有效的时间信用分配；（2）在长程复杂任务中可能遭受优势坍缩；（3）需要在采样预算和策略性能之间进行昂贵的权衡。在本工作中，我们提出了单次轨迹自回归策略优化（SAPO），一种低内存且计算高效的框架，其中策略和价值函数共享单一的自回归骨干网络。SAPO利用LLM的自回归结构，在共享参数下于不同的因果边界生成策略和价值预测，同时独立优化PPO目标和辅助的在线SARSA目标。为了稳健地估计每一轮的贡献，我们进一步引入了结合λ回报与批归一化的轨迹级广义优势估计器。在ALFWorld和WebShop上使用Qwen2.5-1.5B/7B进行的实验表明，SAPO训练稳定，平均比PPO和GRPO分别高出15.1和12.1个百分点，同时消除了单独评论家模型的内存成本，并将每轮迭代运行时间相比PPO减少了33.2%。

## Abstract
Agentic reinforcement learning (RL) has become a critical stage in the post-training of large language models. Existing critic-free, group-relative methods estimate policy advantages from multiple rollouts, avoiding the substantial memory overhead of conventional proximal policy optimization (PPO) and achieving strong performance on long-horizon interactive tasks. Despite their success, recent studies revealed three limitations: (1) Lack explicit value generalization and effective temporal credit assignment; (2) Suffer from potential advantage collapse in long-horizon complex tasks; (3) Require a costly trade-off between sampling budget and policy performance. In this work, we propose Single-rollout Autoregressive Policy Optimization (SAPO), a low-memory and compute-efficient framework in which the policy and value functions share a single autoregressive backbone. SAPO exploits the autoregressive structure of LLMs to produce policy and value predictions at distinct causal boundaries with shared parameters, while independently optimizing the PPO objectives and auxiliary on-policy SARSA objectives. To robustly estimate the contribution of each turn, we further introduce a trajectory-level generalized advantage estimator that combines lambda-returns with batch normalization. Experiments across ALFWorld and WebShop with Qwen2.5-1.5B/7B show that SAPO trains stably and outperforms PPO and GRPO by mean +15.1 and +12.1 percentage points, respectively, while eliminating the memory cost of a separate critic model and reducing per-iteration runtime by 33.2% over PPO.

---

## 论文详细总结（自动生成）

## 一、论文核心问题与整体含义（研究动机和背景）

- **研究背景**：智能体强化学习（Agentic RL）已成为大语言模型（LLM）后训练的关键阶段，使模型能够在长程交互任务（如推理、工具调用、网页搜索、代码执行）中学习决策。
- **现有方法的两难**：
  - **PPO 类 actor-critic 方法**：提供显式价值函数和时序信用分配，但需要独立的 policy-scale critic，带来巨大内存和计算开销。
  - **无 critic 的组相对方法（如 GRPO）**：通过多次 rollout 归一化奖励来估计优势，省去 critic，但存在三个局限：
    1. 缺乏显式价值泛化和有效的时间信用分配；
    2. 稀疏奖励和长程失败场景下，组内奖励相同时会发生优势坍缩（advantage collapse）；
    3. 采样预算与性能之间存在昂贵权衡：小组噪声大，大组成本高且带来同步开销。
- **论文要解决的核心问题**：能否在不维护独立 critic、也不依赖多 rollout 分组采样的前提下，同时获得显式价值学习、时序信用分配、低内存和高效训练？
- **整体含义**：SAPO 提出一种单次 rollout 的自回归 actor-critic 框架，让策略与价值函数共享同一 LLM 骨干，在因果边界处分别输出策略、状态价值和动作价值，从而兼顾性能与效率。

## 二、论文提出的方法论：核心思想、关键技术细节

### 核心思想
- **利用自回归因果结构**：在一个语言模型中，按“状态 → 动作 → 动作后评估”的顺序，在因果边界处读取不同类型的信息：
  - 在状态上下文 `cst` 末尾读取状态价值 `V(st)`；
  - 在状态 + 动作上下文 `[cst; at]` 末尾读取动作价值 `Q(st, at)`；
  - 策略生成则发生在两者之间。
- 这种设计使价值估计与动作生成共享参数，同时通过因果掩码保证信息隔离，无需独立 critic 或手动切分表征。

### 关键技术细节
1. **两 token 价值基（two-token value basis）**：
   - 从词汇表保留两个特殊 token `w+` 和 `w−`，不用于生成，仅用于价值读取。
   - 价值标量由 logit 差定义并裁剪到 `[-1, 1]`：
     \[
     p_\theta(c) = \mathrm{clip}\left(\frac{z_{\theta,w^+}(c)-z_{\theta,w^-}(c)}{\tau_v}, -1, 1\right)
     \]
   - 再乘以 `R_max` 得到 `V` 和 `Q`。
2. **策略与价值分离**：策略词汇表排除 `w+/w−`，在动作 softmax、rollout、log-prob 和熵计算中均不使用这两个 token，避免价值基干扰策略学习。
3. **单 rollout 轨迹优势估计**：
   - 使用轨迹级广义优势估计（GAE）：
     \[
     \delta_t = r_t + \gamma(1-d_t)V^{old}_{t+1} - V^{old}_t
     \]
     \[
     A^{GAE}_t = \delta_t + \gamma\lambda(1-d_t)A^{GAE}_{t+1}
     \]
   - 状态价值目标：\(y^V_t = V^{old}_t + A^{GAE}_t\)（λ-return）；
   - 动作价值目标：\(y^Q_t = r_t + \gamma(1-d_t)Q^{old}_{t+1}\)（on-policy SARSA 目标）。
4. **批归一化优势**：在批次内对有效 turn 的优势做均值和方差归一化，再广播给该 turn 的所有 token，避免长回复主导优势统计，且不依赖同 prompt 的多 rollout。
5. **联合优化目标**：
   - 策略损失：PPO 式 clipped surrogate，按 token 计算；
   - 价值损失：对 `V` 和 `Q` 使用 PPO 风格的 clipped 回归损失；
   - 附加参考策略 KL 惩罚和熵正则。
   - 总损失：
     \[
     L_{SAPO} = L_{pol} + c_V L_V + c_Q L_Q + \beta L_{KL} - c_H H(\pi_\theta)
     \]
   - 三个目标共享 transformer 和 language model head，在一次反向传播中完成更新。

## 三、实验设计

### 数据集 / 场景
- **ALFWorld**：具身家庭环境，长程文本推理和决策，3,827 个任务，分为 6 个类别（Pick、Look、Clean、Heat、Cool、Pick2）。
- **WebShop**：交互式网页购物环境，约 110 万商品、12,000 条用户指令。

### 对比方法
- **闭源模型（prompting）**：GPT-4o、Gemini-2.5-Pro；
- **无训练 prompting 智能体**：ReAct、Reflexion；
- **RL 方法**：PPO、GRPO、RLOO、EMPG、GiGPO（含 w/ std 和 w/o std 两种变体）。

### 基础模型
- Qwen2.5-1.5B-Instruct、Qwen2.5-7B-Instruct。

### 实验覆盖
- 每个 RL 方法在 ALFWorld 和 WebShop 上各报告结果，并给出 3 个随机种子的均值和标准差（部分 baseline 缺少标准差）。
- 额外报告了 per-iteration runtime 对比（PPO vs SAPO）。

## 四、资源与算力

- 文中明确说明所有实验使用 **4 台 NVIDIA H200 和 8 台 NVIDIA A40**。
- 训练步数：ALFWorld 最大交互轮数 50，WebShop 最大 15；整体训练 150 步。
- 未披露：具体训练时长（小时数）、每种实验占用的 GPU 数量、批大小、参数总量等细节。
- 因此，仅能确认硬件配置，无法评估总计算成本。

## 五、实验数量与充分性

- **实验数量**：
  - 两个 benchmark（ALFWorld、WebShop）；
  - 两个模型规模（1.5B、7B）；
  - 三个种子重复；
  - 一次 runtime 分解对比；
  - 论文研究问题中提到对组件敏感性和消融分析，但从提供的文本内容中**未见具体消融实验结果**，可能被省略或放在补充材料中。
- **充分性评价**：
  - 优点：覆盖了多元基线和两类典型长程智能体任务，对比了主流 PPO/GRPO 及其变体，结果具有较强说服力。
  - 不足：缺少详细的消融表（如去掉 Q 目标、去掉批归一化、不同 λ/γ、不同价值温度等）；未与 DAPO、VAPO、VinePPO 等近期方法直接对比；未报告训练曲线或稳定性量化指标（如梯度范数、价值估计误差）；部分 baseline 没有标准差，难以完全判断显著性。

## 六、论文的主要结论与发现

1. **性能提升**：SAPO 在 ALFWorld 和 WebShop 上，使用 Qwen2.5-1.5B/7B，平均比 PPO 高 15.1 个百分点，比 GRPO 高 12.1 个百分点。
2. **具体例子**：
   - Qwen2.5-1.5B：ALFWorld 总成功率 90.1%，比 PPO 高 35.7 点、比 GRPO 高 17.3 点；Clean 和 Heat 类别达到 100%。
   - Qwen2.5-7B：ALFWorld 成功率 94.0%，WebShop 得分 88.6、成功率 82.4%，均优于 PPO 和 GRPO 以及多个较新变体。
3. **效率提升**：SAPO 省去独立 critic 模型的内存，比 PPO 每轮迭代运行时减少 33.2%（从 451.2 s 降至 301.4 s），主要节省来自轨迹生成和 critic 推理/更新。
4. **稳定性**：仅使用每 prompt 一个 rollout，训练稳定，未观察到优势坍缩。
5. **通用性**：性能提升不局限于特定模型容量或环境，说明自回归结构与 actor-critic 对齐具有普适价值。

## 七、优点（方法或实验设计上的亮点）

- **方法创新性强**：利用 LLM 自回归生成的因果边界同时表示策略、状态价值和动作价值，是概念上优雅且计算上高效的架构设计。
- **避免 critic 复制**：单一骨干共享参数，减少内存和计算，同时保留显式价值学习和时间信用分配。
- **单 rollout 可行性**：通过轨迹级 GAE + 批归一化，突破了对多 rollout 分组归一化的依赖，降低了采样成本。
- **价值与策略解耦**：通过排除特殊 token 和独立损失目标，避免了策略与价值学习的互相干扰。
- **实验设置较公平**：使用相同基础模型、相同超参数框架，与多个代表性 baseline 对比，且报告多随机种子结果。
- **效率证据完整**：提供了运行时分解，明确指出节省来源，有利于可复现性。

## 八、不足与局限

- **实验范围有限**：仅涉及 ALFWorld 和 WebShop，未覆盖代码生成、数学推理、通用工具调用等更广泛场景，外部效度有限。
- **缺少消融细节**：论文声称研究组件贡献，但正文未见系统性消融实验，难以判断各组件（如 Q 目标、批归一化、价值温度、λ/γ）的具体贡献。
- **训练规模较小**：150 训练步数、1.5B/7B 模型，可能不足以代表大规模 RL 场景中的行为。
- **潜在偏差风险**：
  - 某些 baseline 结果来自其他论文（如 GiGPO），可能因训练细节、种子、评估协议不同而不完全公平；
  - 部分 baseline 未报告标准差，统计严谨性不足。
- **方法限制**：
  - 价值读取依赖 `R_max` 和温度 `τ_v`，这两个超参数对任务奖励标定敏感，论文未深入讨论其影响；
  - 两个保留 token 的 logit 差仅能表达有限范围内的标量价值，表达力可能弱于完整 critic head；
  - 未讨论与 DAPO、VAPO 等近期先进方法的直接对比，也未说明在长 CoT 或复杂推理任务上的表现。
- **计算成本透明度不足**：未给出总 GPU 小时数、批大小、吞吐量等，难以评估实际资源需求。

（完）
